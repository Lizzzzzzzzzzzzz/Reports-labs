~~~~
                                                    Лабораторна робота №8     

Автор:Прохорчук Дмитро Сергійович
Група:ІПЗ-13
Варіант:18

a.	Створити словник, який має декілька ключів та значень. Кожному ключу відповідає масив таких елементів: прізвище студента, курс, три оцінки в сесію. Надрукувати словник. Реалізувати такі запити: 1) надрукувати список студентів, які не мають оцінок нижче чотирьох; 2) надрукувати список студентів, які мають усі відмінні оцінки; 3) визначити, чи є студенти з однаковим середнім балом.
b.	На уроці літератури вчитель вирішив дізнатися, хто з 40 учнів читав книги А, В і С. Результати опитування виглядають так: А - 25 уч., В - 22 уч., З - 22 уч .; одну з книг А або В - 33 уч., А або С - 32 уч., В або С - 31 уч, все три книги прочитали 10 учнів. Скласти програму, яка визначає, скільки учнів прочитали по одній книзі? Скільки учнів прочитали рівно 2 книги? Скільки учнів не прочитали жодної книги? 



d = {1: ["Ivanov", 3, [4, 4, 4]], 2: ["Petrov", 3, [5, 5, 5]], 3: ["Sidorov", 3, [5, 5, 5]]}
result = set()
def findGoodMarks():
    for key, value in d.items():
        for k in range(0, 2):
            if (value[2][k] >= 4):
                result.add(value[0])
    return result

#print(findGoodMarks())

result2 = []
def findPerfectMarks():
    for key, value in d.items():
        res = 0
        for k in range(0, 3):
            res += value[2][k]
            if (res == 15):
                result2.append(value[0])
    return result2

#print(findPerfectMarks())

d2 = dict()
def findSameSurnames():
    res2 = 0
    for key, value in d.items():
        d2[value[0]] = value[2]
    for key1, value1 in d2.items():
        res2 = 0
        for nums in value1:
            res2 += nums
            d2[key1] = res2
    return d2

print(findSameSurnames())